# LinuxDO 签到配置文件
# 优先级：环境变量 > 此配置文件

# ========== 账号配置 ==========
# 用户名（可选，首次登录后会保存登录状态）
username: ""
# 密码（可选）
password: ""
# Cookie（备选登录方式，可选）
cookie: ""

# ========== 浏览器配置 ==========
# 用户数据目录（保存登录状态）
# 默认: ~/.linuxdo-browser/
user_data_dir: ""

# 是否无头模式（默认 false，有头模式）
# 有头模式 CF 验证通过率更高
# 无图形界面的服务器建议设为 true
headless: false

# 浏览器路径（可选，留空自动检测）
# Linux 常见路径:
#   /usr/bin/chromium-browser (Ubuntu/Debian)
#   /usr/bin/chromium (Arch/Fedora)
#   /snap/bin/chromium (Snap 安装)
#   /usr/bin/google-chrome (Chrome)
browser_path: ""

# Chrome 额外启动参数（可选）
#
# 重要说明：
# - Linux 系统会自动添加 --no-sandbox 等必要参数
# - 通常不需要手动配置此项
# - 如果浏览器仍然启动失败，可以尝试添加以下参数
#
# 常用参数:
#   --no-sandbox          Linux 必需（已自动添加）
#   --disable-dev-shm-usage  避免 /dev/shm 空间不足（已自动添加）
#   --disable-gpu         禁用 GPU 加速（已自动添加）
#   --headless=new        新版无头模式
#   --remote-debugging-port=9222  指定调试端口
#
# 示例:
#   chrome_args:
#     - "--no-sandbox"
#     - "--disable-dev-shm-usage"
#     - "--disable-gpu"
chrome_args: []

# ========== 签到配置 ==========
# 浏览帖子数量
browse_count: 10

# 点赞概率（0-1，0.3 表示 30%）
like_probability: 0.3

# 浏览间隔（秒）
browse_interval_min: 15
browse_interval_max: 30

# ========== Telegram 通知 ==========
# Bot Token（从 @BotFather 获取）
tg_bot_token: ""

# Chat ID（从 @userinfobot 获取）
tg_chat_id: ""
